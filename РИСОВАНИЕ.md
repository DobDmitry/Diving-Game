# Карта рисующего слоя

Всё, что рисует, живёт в `app/src/main/assets/art.js`. Игровая логика — в
`index.html`. Разделены специально: правки визуала не должны трогать физику.

| Файл | Что внутри | Трогать при работе над графикой |
|---|---|---|
| `art.js` | палитра, градиенты, скелет, зал, вышка, вода, трибуны, судьи, эффекты, камера | **да, только его** |
| `index.html` | таблица прыжков, физика, судейство, звук, управление, цикл | нет |
| `bench.html` | стенд для просмотра | по желанию |

`art.js` подключается первым обычным `<script src>`, поэтому делит с игрой общую
область видимости — обращения к `state`, `y`, `rot`, `sel`, `dives` работают напрямую,
никаких импортов и экспортов не нужно.

## Стенд

Откройте `bench.html` — он подключает тот же `art.js` и показывает контрольный лист:
все позы рядом, вышка целиком, вода, трибуны, судьи, сцена в четырёх ракурсах.
Игровой логики там нет, состояние подставлено заглушками, поэтому со стенда
сломать игру невозможно.

Ползунки поворота и масштаба, галочка анимации (волны, толпа, брызги), кнопка
«брызги» для всплеска и «перечитать art.js» после правки.

Цикл работы: правите `art.js` → обновляете стенд → смотрите. Игра при этом
не открывается и не перепроверяется вовсе.

---

## 1. Система координат

Сцена рисуется в «мире» **420 × 680** условных единиц, независимо от размера экрана.

```
const W=420, H=680, PXM=46, WY=560, EDGE=134;
```

- `PXM = 46` — сколько единиц в одном метре. Отсюда 10 м = 460 единиц.
- `WY = 560` — уровень воды.
- `m2y(h)` — перевод «высота в метрах над водой» → координата Y. `m2y(10) = 100`.
- `EDGE = 134` — край десятиметровой площадки.

Опорные высоты: 10 м → Y=100, 7.5 м → 215, 5 м → 330, 3 м → 422, бортик → 552.

### Камера

`draw()` ставит трансформацию так, что точка `cam.cx, cam.cy` оказывается в центре
экрана с увеличением `cam.z`. Целевые значения задаёт `camTarget()`,
сглаживание — в `loop()`.

Глобальный `vis = {x0, y0, w, h}` — прямоугольник мира, реально видимый в кадре.
**Все фоновые слои обязаны рисоваться по `vis`, а не по 0…420**, иначе при отъезде
камеры по краям появятся дыры. Пример в `drawHall()`.

---

## 2. Порядок слоёв

`draw()`. Порядок принципиален: каждый слой подкрашивает предыдущие.

1. `drawHall()` — небо, фермы, светильники, задняя стена, окна, табло
2. `drawStands()` — трибуны и зрители
3. **дымка воздушной перспективы** — полупрозрачная заливка поверх дальнего плана
4. бортик бассейна (`GR.deck`)
5. `drawJudges()` — судьи, столы, карточки
6. `drawWater()` — вода, каустики, линия поверхности
7. `drawReflection()` — отражение спортсмена
8. подводная часть спортсмена (с отсечением по линии воды)
9. `drawTower()` — вышка, трамплин, лестница
10. `drawFx()` — брызги, пена, пузыри
11. `drawRipCue()` — метка окна входа
12. спортсмен в полёте / в стойке
13. `drawHud()` — счётчик оборотов (в координатах мира, без камеры)
14. виньетка (в координатах устройства)

Дымка на шаге 3 — тот самый слой, из-за которого нельзя доводить фон в изоляции:
он осветляет всё, что нарисовано до него.

---

## 3. Палитра и градиенты

Градиенты кэшируются один раз в `buildGrads()`, потому что координаты
мира статичны. Если меняете цвет — правьте там, а не в местах использования.

| Ключ | Что красит |
|---|---|
| `GR.hall` | небо зала, вертикальный градиент 0…WY |
| `GR.water` | вода, от поверхности на 260 единиц вглубь |
| `GR.col` | ствол вышки поперёк, светлая грань → тёмная |
| `GR.slab` | плита площадки |
| `GR.glass` | стекло ограждения |
| `GR.deck` | бортик бассейна |
| `GR.wall` | задняя стена |
| `GR.ao` | затенение под нависающей плитой |
| `GR.win` | окно |
| `GR.board` | дюраль трамплина |
| `GR.vig` | виньетка, единственный в координатах устройства |

Отдельные цвета: `SKIN`, `SKIN2` (дальняя сторона тела), `OUTL` (контур),
`SUIT`, `HAIR` — строки 171 и 314.

---

## 4. Спортсмен

### Скелет

`SEG` — длины сегментов. `POSES` — позы как наборы углов.
Углы в градусах, ноль = вертикаль, положительное направление — вперёд (в сторону лица).

| Канал | Смысл | 0 означает |
|---|---|---|
| `torso` | наклон корпуса | вертикально |
| `spine` | грудной отдел относительно корпуса | прямая спина |
| `head` | голова относительно груди | продолжение линии |
| `sh` | плечо | руки вверх, «стрелочка» |
| `el` | локоть | прямая рука |
| `hip` | сгиб в тазу | прямое тело |
| `knee` | колено | прямая нога |
| `ank` | стопа | оттянутый носок |
| `armF` | ракурсное сокращение рук | 1 = рука в плоскости экрана |

Позы: `stand`, `hopFw`, `squatBk`, `line`, `openFw` (уголок), `openBk` (лодочка),
`tuck`, `pike`.

Переходы — `setPose(поза, длительность, раскладка)`. Раскладка (`OPEN_ST`,
`OPEN_ST_LEGS`, `CLOSE_ST`, `TUCK_ST`) задаёт для каждого сустава пару
`[задержка, длительность]` — этим и получается, что ноги идут первыми, а корпус за ними.

### Отрисовка

`drawBody(x, y, уголМира, f, поза, точкаВращения)`. `f = -1` зеркалит
фигуру (классы 2 и 4). Порядок внутри:

дальняя нога и рука (тоньше, цвет `SKIN2`) → корпус → дельты → теневая грань и линия
грудных → ближняя нога (контур, затем заливка) → плавки → шея → голова →
**ближняя рука последней, поверх головы**.

Приёмы: `limbT()` — конус с округлыми концами; `musc()` — два конуса с утолщением
в брюшке мышцы; контур делается тем же вызовом с прибавкой к толщине (`OW`) и цветом
`OUTL` **под** заливкой.

---

## 5. Вода

`waveY(px)` — профиль поверхности, сумма двух синусов. Используется и для
заливки, и для линии блика — при правке меняйте в одном месте.

`drawWater()`: заливка `GR.water` по профилю → каустики (пять полос,
глубже — слабее) → линия поверхности → широкий мягкий блик под ней.

Брызги — `spawnSplash(x, сила)` и `drawFx()`. Четыре системы:
`foam` (пенное пятно), `crown` (корона по краям), `jets` (центральный столб),
`drops` (капли с гравитацией, вытянутые по вектору скорости), плюс `rings` и `bubbles`.

**Важно:** все частицы обновляются с коэффициентом `kp = dts*60`, а не покадрово.
Иначе в слоу-мо перед входом брызги улетают на полной скорости.

---

## 6. Вышка

`drawTower()`. Составные части вынесены в отдельные функции:

- `stairs(верх, низ)` — лестничная шахта слева, x 2…20
- `slab(x, ширина, верх, толщина)` — плита с нескользящим покрытием
- `ao(x, ширина, верх)` — затенение под плитой
- `gusset(x, верх, длина, спуск)` — консольная косынка
- `glassRail(x1, x2, верх)` — стеклянное ограждение
- `springboard(y3)` — площадка 3 м, анкер, каретка, доска

Ствол — x 20…74, книзу расширяется. Крапинка на плитах берётся из массива `GRIT`
— фиксированный «шум», а не случайный каждый кадр, иначе покрытие будет кипеть.

---

## 7. Трибуны и судьи

Зрители — массив `crowd`, у каждого свой оттенок кожи, причёска, одежда,
масштаб, наклон и фаза покачивания. Рисуются в `drawStands()` с отсечением
по `vis` — за кадром не рисуются вовсе, это держит частоту кадров.

Судьи — `drawJudge()`, устроены так же, но подробнее: две руки, шея, поворот
головы за спортсменом, поднятие карточки по `cardU(i)`.

---

## 8. Три шкалы времени

Легко перепутать, поэтому запомните:

- `dt` — реальные секунды кадра. По ним живут волны, покачивание толпы, вспышки.
- `dts = dt * slow` — секунды анимации. По ним идут переходы поз и частицы.
- `s = dts * 0.62` — секунды физики. По ним летит спортсмен.

`slow` падает до ~0.4 перед входом и под водой. Всё, что должно замедляться вместе
с игрой, обязано считаться по `dts`, а не по `dt`.

---

## 9. Как быстро смотреть результат

Откройте файл в браузере, F12 → Console, вставьте:

```js
window.__show=(names,zoom,rotDeg,f)=>{window.requestAnimationFrame=()=>0;
 setTimeout(()=>{ctx.setTransform(1,0,0,1,0,0);
  ctx.fillStyle="#E8F1FA";ctx.fillRect(0,0,cv.width,cv.height);
  const s=(view.s||1)*(zoom||4.5),n=names.length;
  names.forEach((nm,i)=>{const p=POSES[nm];
   ctx.setTransform(s,0,0,s,cv.width*(i+0.5)/n,cv.height*0.45);
   drawBody(0,0,(rotDeg||0)*Math.PI/180,f||1,p,comOf(p));
   ctx.setTransform(1,0,0,1,0,0);ctx.fillStyle="#0B2545";ctx.font="14px sans-serif";
   ctx.fillText(nm,cv.width*(i+0.5)/n-24,cv.height*0.9);});},80);
 return "ok";};
```

Дальше `__show(["pike","openFw","line"], 4)` — позы крупно и рядом.
Третий аргумент поворачивает фигуру, четвёртый (`-1`) зеркалит.

Чтобы разглядеть сцену, остановите цикл и подвиньте камеру:

```js
window.requestAnimationFrame=()=>0;
cam.z=3; cam.cx=95; cam.cy=340; draw();
```

После этого страницу надо перезагрузить, чтобы игра ожила.

**Замер частоты кадров.** Если вкладка не на переднем плане, браузер режет
`requestAnimationFrame` примерно до 1 кадра в секунду. Это не тормоза графики —
не пугайтесь, измеряйте только на активной вкладке.

---

## 10. Чего лучше не трогать

Весь `index.html`: таблица КТ, скорости крутки, зоны отталкивания, калибровка
момента раскрытия, судейство, обработка нажатий. Графика с ним почти
не пересекается, но `comOf()` из `art.js` используется и как точка вращения в полёте —
если будете менять длины сегментов в `SEG`, центр тяжести уедет и фигура начнёт
вращаться вокруг другой точки.

Пропорции подобраны от вида сбоку: ширины вдвое меньше фронтальных, иначе спортсмен
выглядит квадратным. Это первое, обо что легко споткнуться.
